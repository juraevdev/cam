# Smart Gate — Frontend
#
#   npm install
#   npm run dev      # http://localhost:5173
#   npm run build
#
# Env (optional):
#   VITE_API_BASE_URL=http://localhost:8000
#   VITE_GATE_WS_URL=ws://localhost:8000/ws/logs/
#   VITE_CAMERA_STREAM_URL=http://127.0.0.1:8081/stream
#
# Live camera: start CV scanner (publishes MJPEG on :8081), then open the dashboard.
#
